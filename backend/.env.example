# Server Configuration
PORT=8000

# CORS Configuration
ALLOWED_ORIGINS=chrome-extension://*,http://localhost:3000

# Test Configuration (only needed for running tests)
TEST_API_KEY=your_gemini_api_key_here

# Note: Gemini API keys are provided by users through the extension
# and passed with each request, so no API key is needed in the .env file
# for production use.
